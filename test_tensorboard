from torch.utils.tensorboard import SummaryWriter

writer = SummaryWriter("logs")

for i in range(100):
    writer.add_scalar("y=x", 2*i, i)

writer.close()

#要了解一个类的使用方法，可以按ctrl键然后点击这个类，就会出现说明
#summarywriter这个类定义时输入一个字符串，则该字符串为它运行时会创建的文件夹的名称。
#运行后要打开pycharm的终端，输入tensorboard --logdir=文件夹名称 --port=数字
#后面的数字是给图片打开指定一个地址，防止多人使用默认地址产生冲突
#若想多次画图，将同名文件夹的文件删除或者新建一个子文件夹避免画图冲突
